es:
  keppler:
    sidebar-menu:
      comanda-control: Comanda control
      comanda-control-submenu:
        tables: Mesas
        employees: Mesoneros
    permissions:
    models:
      singularize:
        order: order
        employee: mesonero
        table: mesa
      pluralize:
        orders: orders
        employees: mesonero
        tables: mesas
    modules:
      admin/order: Order
      admin/employees: Mesoneros
      admin/table: Mesa
    messages:
    actions:
  activerecord:
    attributes:
    errors:
      messages:
  errors:
  helpers:
