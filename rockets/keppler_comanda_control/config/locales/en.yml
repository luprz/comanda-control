en:
  keppler:
    sidebar-menu:
      comanda-control: Comanda control
      comanda-control-submenu:
        table: Table
    permissions:
    models:
      singularize:
        order: order
        employee: employee
        table: table
      pluralize:
        orders: orders
        employees: employees
        tables: tables
    modules:
      admin/order: Order
      admin/employees: Employees
      admin/table: Table
    messages:
    actions:
  activerecord:
    attributes:
    errors:
      messages:
  errors:
  helpers:
